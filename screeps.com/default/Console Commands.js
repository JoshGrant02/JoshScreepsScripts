//Spawn

Game.spawns['JoshSpawn'].createCreep([WORK, WORK, WORK, CARRY, MOVE, MOVE, MOVE], undefined, {role: 'upgrader'});


for(var key in Game.)

Game.creeps[]

Game.creeps.Asher.room.find(FIND_SOURCES)



var stuff = Game.getObjectById('5982fc5fb097071b4adbd47a'); console.log(stuff)

var creep = Game.creeps.Cameron; creep.memory.sourceId = creep.pos.findClosestByRange(FIND_SOURCES).id; const source = Game.getObjectById(creep.memory.sourceId);

var creep = Game.creeps.Cameron; 

var sourceID = creep.pos.findClosestByRange(FIND_SOURCES).id; 

Game.getObjectById(sourceID)



console.log(sourceID); 

Game.creeps.Lily.moveTo(31, 21)



//move nonworking builders?

if(creep.build(targets) == ERR_INVALID_TARGET) {
    creep.moveTo(36, 45)
}